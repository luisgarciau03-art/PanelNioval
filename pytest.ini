[pytest]
# Ancla el rootdir a PanelNioval (evita que pytest suba al pyproject.toml del home
# y camine el directorio de usuario, lo que provocaba errores de colección flaky
# con archivos transitorios como .claude.json.lock).
testpaths = tests
addopts = -q
